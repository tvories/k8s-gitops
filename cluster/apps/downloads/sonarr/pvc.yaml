---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-sonarr-config
  namespace: downloads
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: nfs-sonarr-config
  resources:
    requests:
      storage: 1Mi
