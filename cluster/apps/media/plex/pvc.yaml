---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-plex-config
  namespace: media
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: nfs-plex-config
  resources:
    requests:
      storage: 1Mi
