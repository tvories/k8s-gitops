---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-pvc
  namespace: home
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: nfs-adguard
  resources:
    requests:
      storage: 1Mi
