---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-pvc
  namespace: networking
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: nfs-unifi
  resources:
    requests:
      storage: 1Mi