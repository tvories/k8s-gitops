---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config
  namespace: downloads
  labels:
    triliovault.trilio.io/backup: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: ceph-block

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: prowlarr-nfs-backup
spec:
  storageClassName: prowlarr-nfs-backup
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  nfs:
    path: /mnt/fatguys/Backup/k8s/applications/prowlarr
    server: ${SECRET_NAS1}
  mountOptions:
    - nfsvers=4.1
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
    - rsize=1048576
    - wsize=1048576
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-nfs-backup
  namespace: downloads
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: prowlarr-nfs-backup
  resources:
    requests:
      storage: 1Mi
