---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: selfhosted
resources:
  - ocirepository.yaml
  - ./pvc.yaml
  # Disabled in favour of paperless-ngx's built-in AI (PAPERLESS_AI_* env on
  # the paperless HelmRelease). Only the HelmRelease is removed -- the PVC and
  # volsync entries stay so the existing data survives, and re-enabling is a
  # one-line revert. Flux will prune the HelmRelease, which uninstalls the app.
  # - ./helmrelease.yaml
  - ./volsync.yaml
  # - ./externalsecret.yaml
# patchesStrategicMerge:
#   - patches/postgres.yaml
