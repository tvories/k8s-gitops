upapiVersion: v1
kind: Secret
metadata:
    name: tandoor-postgres-secret
    namespace: selfhosted
type: Opaque
stringData:
    superUserPassword: ${TANDOOR_PG_PASSWORD}
    replicationUserPassword: ${TANDOOR_PG_REPLICATION_PASSWORD:}
