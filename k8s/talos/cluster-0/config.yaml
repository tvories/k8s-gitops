---
name: cluster-0

nodes:
  - hostname: talos-cp-1
    domain: ${domains.hardware}
    ip_address: 192.168.80.91
    controlplane: true
  - hostname: talos-cp-2
    domain: ${domains.hardware}
    ip_address: 192.168.80.92
    controlplane: true
  - hostname: talos-cp-3
    domain: ${domains.hardware}
    ip_address: 192.168.80.93
    controlplane: true
  - hostname: talos-worker-1
    domain: ${domains.hardware}
    ip_address: 192.168.80.94
  - hostname: talos-worker-2
    domain: ${domains.hardware}
    ip_address: 192.168.80.95
  - hostname: physical2
    domain: ${domains.hardware}
    ip_address: 192.168.80.90

controlplane:
  endpoint: "https://talos-prod.${domains.hardware}:6443"

  configPatches:
    - op: add
      path: /machine/kubelet/extraArgs
      value:
        rotate-server-certificates: "true"
        feature-gates: GracefulNodeShutdown=true,MixedProtocolLBService=true

    - op: add
      path: /machine/time
      value:
        disabled: false
        servers:
          - us.pool.ntp.org

    - op: add
      path: /machine/network/interfaces
      value:
        - interface: eth0
          dhcp: true

    - op: add
      path: /cluster/proxy/disabled
      value: true

    - op: add
      path: /cluster/apiServer/certSANs
      value:
        - cluster-0.${domains.hardware}

    - op: add
      path: /machine/certSANs
      value:
        - cluster-0.${domains.hardware}

    - op: add
      path: /cluster/network/cni
      value:
        name: "custom"
        urls:
          - "https://raw.githubusercontent.com/tvories/k8s-gitops/feat/infra/k8s/clusters/cluster-0/integrations/cilium-quick-install/quick-install.yaml"

    - op: replace
      path: /cluster/extraManifests
      value:
        - "https://raw.githubusercontent.com/alex1989hu/kubelet-serving-cert-approver/v0.3.0/deploy/ha-install.yaml"

workers:
  configPatches:
    - op: add
      path: /machine/kubelet/extraArgs
      value:
        rotate-server-certificates: "true"

    - op: add
      path: /machine/time
      value:
        disabled: false
        servers:
          - us.pool.ntp.org

    - op: add
      path: /machine/sysctls
      value:
        fs.inotify.max_user_watches: "1048576"
        fs.inotify.max_user_instances: "8192"

    # - op: add
    #   path: /machine/network/interfaces
    #   value:
    #     - interface: eth0
    #       dhcp: true
